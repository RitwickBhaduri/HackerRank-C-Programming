#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	
    int n,t,s=0;
    scanf("%d", &n);
    while(n>0)
    {
        t=n%10;
        s+=t;
        n/=10;
    }
    printf("%d",s);
    return 0;
}
